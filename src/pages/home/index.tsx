import * as React from 'react';
import Button from '@mui/material/Button';
import Box from '@mui/material/Box';

const Home: React.FunctionComponent = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Button>Hackthon03</Button>
    </Box>
  );
};

export default Home;
